// Given a string s, find the length of the longest substring without duplicate characters.

// start and end point maintain a hashset to check if have encountered tha char before

const str="abcabcbb"

function findLongestNonRepeatingSubString(str){
    let left =0
    let charSet=new Set()

    for(let )
}

